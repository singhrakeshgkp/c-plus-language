#ifndef MATH_UTILS // ifndef = if not defined
#define MATH_UTILS
struct Rectangle{
    double length;
    double width;
};

double area(Rectangle rectangle);
double pow(double base, int raise = 2);

#endif